## 下载 交叉编译工具 gcc
    官网：https://developer.arm.com/downloads/-/gnu-a
    https://releases.linaro.org/components/toolchain/binaries/
    https://blog.csdn.net/jinchengzhou/article/details/105087085

export PATH=/home/numax/workspace/aarch64/gcc-linaro-5.1-2015.08-x86_64_aarch64-linux-gnu/bin:$PATH
export PKG_CONFIG_PATH=/home/numax/workspace/depen-static-aarch64/lib/pkgconfig

### 编译OPENSSL
    wget https://github.com/openssl/openssl/archive/OpenSSL_1_1_1-stable.zip
    unzip OpenSSL_1_1_1-stable.zip
    ./config no-asm --cross-compile-prefix=aarch64-linux-gnu- --prefix=/home/numax/workspace/depen-static-aarch64 no-shared
    并删除 Makefile 第 126 行 -m64 参数
    make -j $(nproc) && make install

## 编译x264
    wget https://code.videolan.org/videolan/x264/-/archive/master/x264-master.tar.bz2
    tar -jxvf x264-master.tar.bz2
    cd x264-master
    ./configure --cross-prefix=aarch64-linux-gnu- --host=arm-linux --disable-asm --extra-cflags="-fPIE -pie -fPIC" --enable-static --enable-pic --extra-ldflags="-fPIE -pie -fPIC" --prefix=/home/numax/workspace/depen-static-aarch64
    make -j $(nproc) && make install

## 安装x265
    wget https://bitbucket.org/multicoreware/x265_git/downloads/x265_3.5.tar.gz
    tar -xvf x265_3.5.tar.gz
    cd x265_3.3/source
    mkdir out && cmake -DCMAKE_TOOLCHAIN_FILE="/home/numax/workspace/aarch64/aarch64.toolchain.cmake" -DCMAKE_INSTALL_PREFIX=/home/numax/workspace/depen-static-aarch64 -DENABLE_SHARED=false .. 
    cd out
    make -j $(nproc) && make install
    
    修改：vim /home/numax/workspace/depen-static/lib/pkgconfig/x265.pc 增加 lpthread 并将 private 部分拷贝纸 Libs 依赖
    Libs: -L${libdir} -lx265 -lstdc++ -lpthread -lm -lgcc_s -lgcc -lgcc_s -lgcc -lrt -ldl
    Libs.private: -lstdc++ -lpthread -lm -lgcc_s -lgcc -lgcc_s -lgcc -lrt -ldl

## 安装libvpx
	wget https://github.com/webmproject/libvpx/archive/refs/tags/v1.8.2.tar.gz
	tar -xvf libvpx-1.8.2.tar.gz
	cd libvpx-1.8.2
	CROSS=aarch64-linux-gnu- CFLAGS=-fPIC ./configure --target=arm64-linux-gcc --enable-pic --enable-static  --enable-shared --as=yasm --target=generic-gnu --disable-shared --disable-unit-tests --prefix=/home/numax/workspace/depen-static-aarch64
	make -j $(nproc) && make install

## 安装OpenH264
	git clone https://gitee.com/mirrors/openh264.git
	cd openh264
	make OS=linux ARCH=arm64 CC=aarch64-linux-gnu-gcc CXX=aarch64-linux-gnu-g++
	make install-static-lib PREFIX=/home/numax/workspace/depen-static-aarch64 OS=linux ARCH=arm64 

## 安装 FDK-AAC
    git clone https://gitee.com/nwaycn/fdk-aac.git
    cd fdk-aac && mkdir out && cd out
    cmake .. -DCMAKE_TOOLCHAIN_FILE="/home/numax/workspace/aarch64/aarch64.toolchain.cmake" -DCMAKE_BUILD_TYPE=Release -DBUILD_SHARED_LIBS=OFF -DCMAKE_POSITION_INDEPENDENT_CODE=ON -DCMAKE_INSTALL_PREFIX=/home/numax/workspace/depen-static-aarch64 -DCMAKE_PREFIX_PATH=/home/numax/workspace/depen-static-aarch64
    make -j $(nproc) && make install

## 安装 OPUS
    git clone https://github.com/xiph/opus.git
    cd opus
    ./autogen.sh 
    CC=aarch64-linux-gnu-gcc CXX=aarch64-linux-gnu-g++ LD=aarch64-linux-gnu-ld AR=aarch64-linux-gnu-gcc-ar ./configure --host=arm-linux --enable-shared=no --enable-static=yes --with-pic --prefix=/home/numax/workspace/depen-static-aarch64
    make -j $(nproc) && make install
    修改：vim /home/numax/workspace/depen-static/lib/pkgconfig/opus.pc 14 行 Libs: 增加 -lm

### libjpeg
    wget http://www.ijg.org/files/jpegsrc.v9e.tar.gz
    CC=aarch64-linux-gnu-gcc CXX=aarch64-linux-gnu-g++ LD=aarch64-linux-gnu-ld AR=aarch64-linux-gnu-gcc-ar ./configure --host=arm-linux --prefix=/home/numax/workspace/depen-static-aarch64 --enable-shared=no --with-pic
    make -j $(nproc) && make install

## 安装FFMpeg (--disable-vaapi 未使用硬件加速技术)
    wget http://ffmpeg.org/releases/ffmpeg-4.2.3.tar.xz
    tar -xvf ffmpeg-4.2.3.tar.xz
    cd ffmpeg-4.2.3
    CFLAGS=-fPIC ./configure --pkg-config="pkg-config --static" --cross-prefix=aarch64-linux-gnu- --enable-cross-compile --target-os=linux --cc=aarch64-linux-gnu-gcc --extra-cflags="-fPIE -pie -fPIC -I/home/numax/workspace/depen-static-aarch64/include" --extra-ldflags="-L/home/numax/workspace/depen-static-aarch64/lib" --extra-libs="-ldl -fpic" --prefix=/home/numax/workspace/depen-static-aarch64 --arch=arm64 --enable-pic --disable-shared --enable-static --enable-gpl --disable-vaapi --enable-libx264 --enable-libvpx --enable-libfdk-aac --enable-nonfree --enable-libx265 

    Jetson板上可开启 硬件加速功能（ffmpeg 需要 4.4.x以上版本http://ffmpeg.org/releases/ffmpeg-4.4.3.tar.gz）：
    开启方式：下载nv-codec-headers (n10.0.26.2) 除上面的编译参数外，增加： --enable-cuda --enable-cuvid --enable-ffnvcodec --enable-nvenc --enable-nvdec 
    make -j $(nproc) && make install

## 安装 SRTP2
    下载SRTP2: https://codeload.github.com/cisco/libsrtp/tar.gz/refs/tags/v2.3.0
    tar -xvzf libsrtp-2.3.0.tar.gz
    cd libsrtp-2.3.0
    cmake .. -DCMAKE_TOOLCHAIN_FILE="/home/numax/workspace/aarch64/aarch64.toolchain.cmake" -DCMAKE_INSTALL_PREFIX=/home/numax/workspace/depen-static-aarch64 -DCMAKE_PREFIX_PATH=/home/numax/workspace/depen-static-aarch64 -DBUILD_SHARED_LIBS=false -DENABLE_OPENSSL=true
    cd out
    make -j $(nproc) && make install

## WebRTC DataChannel 依赖库 usrsctp
    git clone https://github.com/sctplab/usrsctp.git
    cd usrsctp
    ./bootstrap
    CC=aarch64-linux-gnu-gcc CXX=aarch64-linux-gnu-g++ LD=aarch64-linux-gnu-ld AR=aarch64-linux-gnu-gcc-ar ./configure --host=arm-linux --prefix=/home/numax/workspace/depen-static-aarch64 --enable-shared=false
    make  -j $(nproc) && make install

## pugixml
    git clone https://github.com/zeux/pugixml.git
    cd pugixml && mkdir build && cd build
    cmake .. -DCMAKE_TOOLCHAIN_FILE="/home/numax/workspace/aarch64/aarch64.toolchain.cmake" -DCMAKE_INSTALL_PREFIX=/home/numax/workspace/depen-static-aarch64 -DCMAKE_INSTALL_PREFIX=/home/numax/workspace/depen-static-aarch64 -DCMAKE_BUILD_TYPE=Release
    make  -j $(nproc) && make install

## libxml2 (https://blog.csdn.net/weixin_42877778/article/details/127060589)
    git clone https://github.com/GNOME/libxml2
    ## autogen.sh 可能会失败，可以执行 cp /usr/share/aclocal/*.m4 /usr/local/share/aclocal/
    cd libxml2 && ./autogen.sh --with-http=no --with-python=no --with-lzma=yes --with-threads=no --disable-shared
    CC=aarch64-linux-gnu-gcc CXX=aarch64-linux-gnu-g++ LD=aarch64-linux-gnu-ld AR=aarch64-linux-gnu-gcc-ar ./configure --host=arm-linux --prefix=/home/numax/workspace/depen-static-aarch64 --enable-shared=false --with-pic --with-python=no --with-zlib=/home/numax/workspace/depen-static-aarch64
    ## --with-zlib=no /home/numax/workspace/depen-static-aarch64
    ./configure CROSS_COMPILE=arm-linux CC=aarch64-linux-gnu-gcc CXX=aarch64-linux-gnu-g++ LD=aarch64-linux-gnu-ld AR=aarch64-linux-gnu-gcc-ar --host=arm-linux --prefix=/home/numax/workspace/depen-static-aarch64 --enable-shared=false --with-pic --with-python=no --with-zlib=no
    make && make install 

## 安装MariaDB 连接器
    依赖curl:
    ./configure CROSS_COMPILE=arm-linux CC=aarch64-linux-gnu-gcc CXX=aarch64-linux-gnu-g++ LD=aarch64-linux-gnu-ld AR=aarch64-linux-gnu-gcc-ar --host=arm-linux --prefix=/home/numax/workspace/depen-static-aarch64 --enable-shared=no --with-pic --with-openssl
    make && make install
    

    git clone https://gitee.com/mirrors/mariadb-connector-c.git
    mkdir out
    cmake .. -DCMAKE_TOOLCHAIN_FILE="/home/numax/workspace/aarch64/aarch64.toolchain.cmake" -DCMAKE_INSTALL_PREFIX=/home/numax/workspace/depen-static-aarch64 -DCMAKE_PREFIX_PATH=/home/numax/workspace/depen-static-aarch64 -DWITH_MYSQLCOMPAT=true
    make && make install
    
		
	将MariaDB改为MySQL客户端 
	cp /home/numax/workspace/depen-static-aarch64/include/mariadb/ /home/numax/workspace/depen-static-aarch64/include/mysql/ -r
	cp /home/numax/workspace/depen-static-aarch64/lib/mariadb /home/numax/workspace/depen-static-aarch64/lib/mysql -r
	cp /home/numax/workspace/depen-static-aarch64/lib/mysql/libmariadbclient.a /home/numax/workspace/depen-static-aarch64/lib/mysql/libmysqlclient.a
	
    vi /etc/profile
    export MYSQL_DIR=/home/numax/workspace/depen-static-aarch64

##	hiredis安装
	git clone https://github.com/redis/hiredis.git
	cd hiredis && mkdir out && cd out
	cmake .. -DCMAKE_TOOLCHAIN_FILE="/home/numax/workspace/aarch64/aarch64.toolchain.cmake" -DCMAKE_INSTALL_PREFIX=/home/numax/workspace/depen-static-aarch64 -DCMAKE_INSTALL_PREFIX=/home/numax/workspace/depen-static-aarch64 -DCMAKE_BUILD_TYPE=Release
	make && make install

## zlib(http://www.zlib.net/zlib-1.2.13.tar.gz)
    cmake .. -DCMAKE_TOOLCHAIN_FILE="/home/numax/workspace/aarch64/aarch64.toolchain.cmake" -DCMAKE_INSTALL_PREFIX=/home/numax/workspace/depen-static-aarch64 -DCMAKE_INSTALL_PREFIX=/home/numax/workspace/depen-static-aarch64 -DCMAKE_BUILD_TYPE=Release -DBUILD_SHARED_LIBS=OFF
    make && make install


## speex 安装
	https://github.com/xiph/speex/archive/refs/tags/Speex-1.2.1.tar.gz
	./autogen.sh 
	CC=aarch64-none-linux-gnu-gcc CXX=aarch64-none-linux-gnu-g++ LD=aarch64-none-linux-gnu-ld AR=aarch64-none-linux-gnu-gcc-ar ./configure --host=arm-linux --prefix=/home/numax/workspace/depen-static-aarch64 --enable-shared=false
	make && make install

## speex-dsp 安装
	https://github.com/xiph/speexdsp/archive/refs/tags/SpeexDSP-1.2.1.tar.gz
	./autogen.sh 
	CC=aarch64-none-linux-gnu-gcc CXX=aarch64-none-linux-gnu-g++ LD=aarch64-none-linux-gnu-ld AR=aarch64-none-linux-gnu-gcc-ar ./configure --host=arm-linux --prefix=/home/numax/workspace/depen-static-aarch64 --enable-shared=false --with-pic
	make && make install

##	libyuv安装 安装
	git clone https://chromium.googlesource.com/external/libyuv
	cd libyuv 
    修改 linux.mk 文件，-fPIC -DLIBYUV_NEON or -DLIBYUV_DISABLE_NEON
    CC=aarch64-none-linux-gnu-gcc CXX=aarch64-none-linux-gnu-g++ LD=aarch64-none-linux-gnu-ld AR=aarch64-none-linux-gnu-gcc-ar make -f linux.mk
    copy inclue libyuv.a


## alsa-lib安装
    wget http://www.alsa-project.org/files/pub/lib/alsa-lib-1.2.8.tar.bz2
    tar -xvf alsa-lib-1.2.8.tar.bz2
    CC=aarch64-none-linux-gnu-gcc CXX=aarch64-none-linux-gnu-g++ LD=aarch64-none-linux-gnu-ld AR=aarch64-none-linux-gnu-gcc-ar ./configure --host=arm-linux --prefix=/home/numax/workspace/depen-static-aarch64
    make && make install

## msys2 + msvc 编译
    准备工作：
        首先执行："C:\Program Files\Microsoft Visual Studio\2022\Community\VC\Auxiliary\Build\vcvars64.bat"
        第二步：进入msys2根目录执行：msys2_shell.cmd -defterm -full-path -no-start -here -mingw64
        export PKG_CONFIG_PATH=/h/depen-static-win/lib/pkgconfig

### 编译OPENSSL
    wget https://github.com/openssl/openssl/archive/OpenSSL_1_1_1-stable.zip
    unzip OpenSSL_1_1_1-stable.zip
    ## 需要使用官方推荐的 ActiveState Perl 生成 (state activate --default yumin11522/Perl-5.36.0-Windows)
    /C/Users/xiongqimin/AppData/Local/ActiveState/cache/bin/perl.exe Configure VC-WIN64A no-asm --prefix=/h/depen-static-win --openssldir=/h/depen-static-win no-shared
    make -j $(nproc) && make install

## 编译x264
    wget https://code.videolan.org/videolan/x264/-/archive/master/x264-master.tar.bz2
    tar -jxvf x264-master.tar.bz2
    cd x264-master
    CC=cl ./configure --disable-asm --enable-static --enable-pic --prefix=/h/depen-static-win
    make -j $(nproc) && make install

## 安装x265
    打开一个新的 vcvars64.bat 窗口
    wget https://bitbucket.org/multicoreware/x265_git/downloads/x265_3.5.tar.gz
    tar -xvf x265_3.5.tar.gz
    cd x265_3.3/source
    mkdir out  && cd out 
    cmake -DCMAKE_INSTALL_PREFIX=h:/depen-static-win -DENABLE_SHARED=false ..
    cmake --build .
    cmake --build . --target INSTALL

## 安装libvpx
	wget -O libvpx-1.30.0.tar.gz https://github.com/webmproject/libvpx/archive/refs/tags/v1.30.0.tar.gz
	tar -xvf libvpx-1.30.0.tar.gz
	cd libvpx-1.30.0
	./configure --enable-static-msvcrt --target=x86_64-win64-vs17 --disable-shared --as=yasm --disable-unit-tests --disable-examples  --prefix=/h/depen-static-win
	make -j $(nproc) && make install

## 安装OpenH264
	git clone https://gitee.com/mirrors/openh264.git
	cd openh264
	make OS=MSVC ARCH=x86_64
	make install-static PREFIX=/h/depen-static-win OS=MSVC ARCH=x86_64

## 安装 FDK-AAC
    git clone https://gitee.com/nwaycn/fdk-aac.git
    cd fdk-aac && mkdir out && cd out
    cmake .. -DBUILD_SHARED_LIBS=OFF -DCMAKE_POSITION_INDEPENDENT_CODE=ON -DCMAKE_INSTALL_PREFIX=h:/depen-static-win -DCMAKE_PREFIX_PATH=h:/depen-static-win
    cmake --build .
    cmake --build . --target INSTALL

## 安装 OPUS
    git clone https://github.com/xiph/opus.git
    cd opus && mkdir out && cd out
    cmake .. -DBUILD_SHARED_LIBS=OFF -DCMAKE_POSITION_INDEPENDENT_CODE=ON -DCMAKE_INSTALL_PREFIX=h:/depen-static-win -DCMAKE_PREFIX_PATH=h:/depen-static-win
    cmake --build .
    cmake --build . --target INSTALL
## 安装FFMpeg (--disable-vaapi 未使用硬件加速技术)
    (需要打 path https://trac.ffmpeg.org/attachment/ticket/9019/0001-Patch-for-ticket-9019-CUDA-Compile-Broken-Using-MSVC.patch)
    wget http://ffmpeg.org/releases/ffmpeg-4.2.3.tar.xz
    tar -xvf ffmpeg-4.2.3.tar.xz
    cd ffmpeg-4.2.3
    ./configure --pkg-config="pkg-config --static" --toolchain=msvc --cc=cl --cxx=cl --ar=lib --ld=link  
        --extra-cflags="-I/h/depen-static-win/include" --extra-ldflags="-L/h/depen-static-win/lib" 
        --prefix=/h/depen-static-win --arch=x86_64 --enable-pic --enable-shared --enable-static 
        --enable-gpl --disable-vaapi --enable-libx264 --enable-libvpx --enable-libfdk-aac 
        --enable-nonfree --enable-libx265 --enable-libopenh264 
        --enable-cuda --enable-cuvid --enable-ffnvcodec --enable-nvenc --enable-nvdec

    如果需要链接到静态链接库(x264)，那么编译前，在保持原始 build.sh 文件不变动的情况下，修改 <ffmpeg_src_dir>/libavcodec/libx264.c 文件，把 36 行注释掉，确保其不生效：
    https://blog.csdn.net/fly4free/article/details/120102205 












### CentOS 交叉编译 Windows 程序过程 <font color="red">废弃</font>
    下载 编译脚本 git clone https://github.com/Zeranoe/mingw-w64-build 需要修改 130 行 mingw-w64 地址为：https://github.com/mirror/mingw-w64.git
    ./mingw-w64-build --prefix=/home/numax/workspace/mingw-w64/output --root=/home/numax/workspace/mingw-w64/source x86_64

    export PATH=/home/numax/workspace/mingw-w64/output/bin:$PATH
    export PKG_CONFIG_PATH=/home/numax/workspace/depen-static-windows/lib/pkgconfig

### 编译OPENSSL
    wget https://github.com/openssl/openssl/archive/OpenSSL_1_1_1-stable.zip
    unzip OpenSSL_1_1_1-stable.zip
    ./Configure mingw64 no-asm --cross-compile-prefix=x86_64-w64-mingw32- --prefix=/home/numax/workspace/depen-static-windows no-shared
    并删除 Makefile 第 126 行 -m64 参数
    make -j $(nproc) && make install

## 编译x264
    wget https://code.videolan.org/videolan/x264/-/archive/master/x264-master.tar.bz2
    tar -jxvf x264-master.tar.bz2
    cd x264-master
    ./configure --cross-prefix=x86_64-w64-mingw32- --host=mingw64 --disable-asm --extra-cflags="-fPIE -pie -fPIC" --enable-static --enable-pic --extra-ldflags="-fPIE -pie -fPIC" --prefix=/home/numax/workspace/depen-static-windows
    make -j $(nproc) && make install

## 安装x265
    wget https://bitbucket.org/multicoreware/x265_git/downloads/x265_3.5.tar.gz
    tar -xvf x265_3.5.tar.gz
    cd x265_3.3/source
    mkdir out && cmake -DCMAKE_TOOLCHAIN_FILE="/home/numax/workspace/mingw-w64/crosscompile.cmake" -DCMAKE_INSTALL_PREFIX=/home/numax/workspace/depen-static-windows -DENABLE_SHARED=false .. 
    cd out
    make -j $(nproc) && make install
    
    修改：vim /home/numax/workspace/depen-static/lib/pkgconfig/x265.pc 增加 lpthread 并将 private 部分拷贝纸 Libs 依赖
    Libs: -L${libdir} -lx265 -lstdc++ -lpthread -lm -lgcc_s -lgcc -lgcc_s -lgcc -lrt -ldl
    Libs.private: -lstdc++ -lpthread -lm -lgcc_s -lgcc -lgcc_s -lgcc -lrt -ldl

## 安装libvpx
	wget https://github.com/webmproject/libvpx/archive/refs/tags/v1.8.2.tar.gz
	tar -xvf libvpx-1.8.2.tar.gz
	cd libvpx-1.8.2
	CROSS=x86_64-w64-mingw32- CFLAGS=-fPIC ./configure --target=x86_64-win64-gcc --enable-pic --enable-static  --enable-shared --as=yasm --target=generic-gnu --disable-shared --disable-unit-tests --prefix=/home/numax/workspace/depen-static-windows
	make -j $(nproc) && make install

## 安装OpenH264
	git clone https://gitee.com/mirrors/openh264.git
	cd openh264
	make OS=mingw_nt ARCH=x86_64 CC=x86_64-w64-mingw32-gcc CXX=x86_64-w64-mingw32-g++
	make install-static PREFIX=/home/numax/workspace/depen-static-windows OS=mingw_nt ARCH=x86_64

## 安装 FDK-AAC
    git clone https://gitee.com/nwaycn/fdk-aac.git
    cd fdk-aac && mkdir out && cd out
    cmake .. -DCMAKE_TOOLCHAIN_FILE="/home/numax/workspace/mingw-w64/crosscompile.cmake" -DCMAKE_BUILD_TYPE=Release -DBUILD_SHARED_LIBS=OFF -DCMAKE_POSITION_INDEPENDENT_CODE=ON -DCMAKE_INSTALL_PREFIX=/home/numax/workspace/depen-static-windows -DCMAKE_PREFIX_PATH=/home/numax/workspace/depen-static-windows
    make -j $(nproc) && make install

## 安装 OPUS
    git clone https://github.com/xiph/opus.git
    cd opus
    ./autogen.sh 
    CC=x86_64-w64-mingw32-gcc CXX=x86_64-w64-mingw32-g++ LD=x86_64-w64-mingw32-ld AR=x86_64-w64-mingw32-ar ./configure --host=mingw64 --enable-shared=no --enable-static=yes --with-pic --prefix=/home/numax/workspace/depen-static-windows
    make -j $(nproc) && make install
    修改：vim /home/numax/workspace/depen-static/lib/pkgconfig/opus.pc 14 行 Libs: 增加 -lm

### libjpeg
    wget http://www.ijg.org/files/jpegsrc.v9e.tar.gz
    CC=x86_64-w64-mingw32-gcc CXX=x86_64-w64-mingw32-g++ LD=x86_64-w64-mingw32-ld AR=x86_64-w64-mingw32-ar ./configure --host=mingw64 --prefix=/home/numax/workspace/depen-static-windows --enable-shared=no --with-pic
    make -j $(nproc) && make install

## 安装FFMpeg (--disable-vaapi 未使用硬件加速技术)
    wget http://ffmpeg.org/releases/ffmpeg-4.2.3.tar.xz
    tar -xvf ffmpeg-4.2.3.tar.xz
    cd ffmpeg-4.2.3
    ./configure --pkg-config="pkg-config --static" --toolchain=msvc --cc=cl --cxx=cl --extra-cflags="-I/h/depen-static-win/include" --extra-ldflags="-L/h/depen-static-win/lib" --prefix=/h/depen-static-win --arch=x86_64 --enable-pic --disable-shared --enable-static --enable-gpl --disable-vaapi --enable-libx264 --enable-libvpx --enable-libfdk-aac --enable-nonfree --enable-libx265 --enable-libopenh264 --enable-cuda --enable-cuvid --enable-ffnvcodec --enable-nvenc --enable-nvdec 

    Jetson板上可开启 硬件加速功能（ffmpeg 需要 4.4.x以上版本http://ffmpeg.org/releases/ffmpeg-4.4.3.tar.gz）：
    开启方式：下载nv-codec-headers (n10.0.26.2) 除上面的编译参数外，增加： --enable-cuda --enable-cuvid --enable-ffnvcodec --enable-nvenc --enable-nvdec 
    make -j $(nproc) && make install

## OGG 安装
    wget https://github.com/xiph/ogg/releases/download/v1.3.5/libogg-1.3.5.tar.gz
    CC=x86_64-w64-mingw32-gcc CXX=x86_64-w64-mingw32-g++ LD=x86_64-w64-mingw32-ld AR=x86_64-w64-mingw32-ar ./configure --host=mingw64 --prefix=/home/numax/workspace/depen-static-windows --enable-shared=false
    make -j $(nproc) && make install
    
## speex 安装
	https://github.com/xiph/speex/archive/refs/tags/Speex-1.2.1.tar.gz
	./autogen.sh 
	CC=x86_64-w64-mingw32-gcc CXX=x86_64-w64-mingw32-g++ LD=x86_64-w64-mingw32-ld AR=x86_64-w64-mingw32-ar ./configure --host=mingw64 --prefix=/home/numax/workspace/depen-static-windows --enable-shared=false
	make -j $(nproc) && make install

## speex-dsp 安装
	https://github.com/xiph/speexdsp/archive/refs/tags/SpeexDSP-1.2.1.tar.gz
	./autogen.sh 
	CC=x86_64-w64-mingw32-gcc CXX=x86_64-w64-mingw32-g++ LD=x86_64-w64-mingw32-ld AR=x86_64-w64-mingw32-ar ./configure --host=mingw64 --prefix=/home/numax/workspace/depen-static-windows --enable-shared=false
	make -j $(nproc) && make install

##	libyuv安装 安装
	git clone https://chromium.googlesource.com/external/libyuv
	cd libyuv 
    mkdir out
    cd out 
    cmake .. -DCMAKE_TOOLCHAIN_FILE="/home/numax/workspace/mingw-w64/crosscompile.cmake" -DCMAKE_BUILD_TYPE=Release -DBUILD_SHARED_LIBS=OFF -DCMAKE_POSITION_INDEPENDENT_CODE=ON -DCMAKE_INSTALL_PREFIX=/home/numax/workspace/depen-static-windows -DCMAKE_PREFIX_PATH=/home/numax/workspace/depen-static-windows
    make -j $(nproc) && make install

## zlib(http://www.zlib.net/zlib-1.2.13.tar.gz)
    cmake .. -DCMAKE_TOOLCHAIN_FILE="/home/numax/workspace/mingw-w64/crosscompile.cmake" -DCMAKE_INSTALL_PREFIX=/home/numax/workspace/depen-static-windows -DCMAKE_INSTALL_PREFIX=/home/numax/workspace/depen-static-windows -DCMAKE_BUILD_TYPE=Release -DBUILD_SHARED_LIBS=OFF
    make && make install

## libdshow
    https://github.com/obsproject/libdshowcapture.git